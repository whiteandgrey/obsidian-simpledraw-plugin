# SimpleDraw Obsidian Plugin — 更新日志 v1.5.7 → v1.6.0

## 概述

v1.6.0 修复了 V 形箭头和圆点箭头在起点/终点的渲染错位问题。

---

## 改动清单

### 1. 示意图

**修复前（v1.5.7）：**

```
V 形终点：  ●──────⚡▽            (path 截 headSize，V 臂有侧向偏移 → 空隙)
V 形起点：  ▽●──────              (path 未截断，从箭头顶点直接伸入)

圆点终点：  ●──────○              (path 截 headSize，圆半径仅 0.4*headSize → 空隙)
圆点起点：  ○-----              (path 未截断)
```

**修复后（v1.6.0）：**

```
V 形终点：  ●──────▽             (path 截 0，直达 V 形顶点)
V 形起点：  ▽──────              (path 向前推出，起点停留在 V 形基底)

圆点终点：  ●─────○              (path 截 0.4*headSize，止于圆边)
圆点起点：  ○──────              (path 向前推出，起点停在圆边)
三角形/空心三角终点/起点均无变化。
```

### 2. 实现

`src/view.ts` — `renderInto()` 辅助函数内部：

- **新增起点截断**：镜像终点逻辑，沿 `first → next` 方向将 `linePts[0]` 向前推出，为起点箭头留出空间
- **截断量据形状调整**：新增 `truncForShape()` 辅助函数

| 形状 | 截断量 |
|------|--------|
| `triangle` / `open-triangle` | `headSize`（不变） |
| `circle` | `headSize * 0.4`（圆半径） |
| `v-shape` | `0`（不截断，线段直达 V 形顶点） |

---

## 修改文件统计

| 文件 | 改动 | 说明 |
|------|------|------|
| `src/view.ts` | ~25 行 | 起点截断 + 截断量据形状调整 |
| `manifest.json` | +1 行 / -1 行 | 版本号 1.5.7 → 1.6.0 |
| `main.js` | 自动 | 构建产物 |

---

## 版本

- 当前版本：1.6.0
- 此前版本：1.5.7
